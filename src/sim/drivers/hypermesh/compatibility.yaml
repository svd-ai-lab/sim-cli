driver: hypermesh
# HyperMesh is part of Altair HyperWorks Desktop. Not pip-installable.
# Install via Altair Units or site license.
sdk_package: null

profiles:
  - name: hypermesh_2025
    solver_versions: ["2025", "2025.1", "2025.1.0"]
    active_sdk_layer: "2025"
    active_solver_layer: "2025"
    notes: |
      HyperWorks Desktop 2025. Python API via `import hm`.
      Execution: hw -b -script script.py (batch, no GUI).
      1946 model methods + 225 entity classes.
      Python 3.x runtime bundled with HyperWorks.

  - name: hypermesh_2024
    solver_versions: ["2024", "2024.1", "2024.2"]
    active_sdk_layer: "2024"
    active_solver_layer: "2024"
    notes: |
      HyperWorks Desktop 2024. Same Python API pattern.
      May have fewer entity classes vs 2025.
